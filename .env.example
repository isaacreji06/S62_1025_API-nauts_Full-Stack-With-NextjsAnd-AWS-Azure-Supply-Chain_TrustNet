# Database (we'll set up PostgreSQL later)
DATABASE_URL="postgresql://postgres:password@localhost:5432/trustnet"

# Authentication (generate proper secrets later)
JWT_SECRET="dev-jwt-secret-change-in-production-2024"
NEXTAUTH_SECRET="dev-nextauth-secret-change-in-production-2024"

# Redis (we'll set up later)
REDIS_URL="redis://localhost:6379"

# Environment
NODE_ENV="development"

# UPI Service (mock for now)
UPI_SERVICE_URL="http://localhost:3001"

# App URL
NEXTAUTH_URL="http://localhost:3000"
